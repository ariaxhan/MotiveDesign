import React from 'react';

const ImageAnalysis = ({ imageScore }) => {
    return (
        <div>
            <h3>Image Analysis</h3>
            <p>Score: {imageScore}</p>
        </div>
    );
};

export default ImageAnalysis;
